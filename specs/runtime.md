# Runtime

- Bash `.sh` scripts
- Linux and macOS MVP
- Bash 3.2 compatibility where practical
- Strict mode in executables and tests
- Standard utilities: `mv`, `rm`, `ln`, `readlink`, `mktemp`, and `date`
- `curl` required only for remote installation and updates
- No compilation or additional runtime
